<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 5.3//EN"
        "http://hibernate.org/dtd/hibernate-configuration-5.3.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/movies</property>
        <property name="connection.username">sri14</property>
        <property name="connection.password">1234</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Automatically create/drop tables -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Specify annotated classes if any -->
        <!-- <mapping class="org.example.YourEntity"/> -->
    </session-factory>
</hibernate-configuration>
